package pages.moduleone;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class ModuleOnePageTwo  extends BasePage {
    WebDriver driver;
    public ModuleOnePageTwo(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
}
